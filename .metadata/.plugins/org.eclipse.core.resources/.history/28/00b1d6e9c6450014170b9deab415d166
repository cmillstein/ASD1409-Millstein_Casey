var mainWin = Ti.UI.createWindow({
	backgroundColor: "#EEE",
	layout: "vertical",
	fullscreen: true
});

for(var i=0, j=api.reddit.data.children.length; i<j; i++){
	var item = api.reddit.data.children[i].data;
	var productInfo = new data.object(item);
}; 

console.log(productInfo);


var data = require("data");
var product = require("product");
var api = require("api");








mainWin.open();
